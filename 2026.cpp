#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;
/*int main() {
	
	string str;
	while (getline(cin,str)) {
		vector<char> v(str.begin(), str.end());
		v[0] = toupper(v[0]);
		for (int i = 1; i < v.size(); i++) {
			if (v[i - 1] == ' ') {
				v[i] = toupper(v[i]);
			}
		}
		for (auto c : v) {
			cout << c;
		}
		cout << endl;
	}
	return 0;
}*/