#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#include <iomanip>
#include<math.h>
using namespace std;

/*int main() {
	int n, target;
	while (cin >>  n >> target&&n||target){
		int len = (int)sqrt(target * 2);
		while (len--)
		{
			int tmp = target / (len + 1) - len / 2;
			if ((2 * tmp + len) * (len + 1) / 2 == target) {
				cout << "[" << tmp << "," << tmp + len << "]" << endl;
			}
		}
		cout << endl;
	}
	return 0;
}*/

