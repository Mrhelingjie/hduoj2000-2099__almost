#include<iostream>
#include<string>
#include<algorithm>
#include <iomanip>
#include<math.h>
using namespace std;

/*int main() {
	long long x,y;
	while (cin >> hex >> x >> y) {
		if (x + y >= 0) {
			cout << setiosflags(ios::uppercase)<< hex << x + y << endl;
		}
		else {
			cout << setiosflags(ios::uppercase) << hex << '-' << -(x + y) << endl;
		}
	}
	return 0;
}*/
