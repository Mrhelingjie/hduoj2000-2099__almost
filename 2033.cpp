#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#include<stack>
using namespace std;

/*int main() {
	int n;
	while (cin >> n) {
		int as, am, ah, bs, bm, bh;
		while (n--) {
			cin >> ah >> am >> as >> bh >> bm >> bs;
			as = as + bs;
			am = am + bm;
			ah = ah + bh;
			am = am + as / 60;
			ah = ah + am / 60;
			as = as % 60;
			am = am % 60;
			cout << ah << " " << am << " " << as << endl;
		}
	}
	return 0;
}*/