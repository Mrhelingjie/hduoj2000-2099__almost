#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#include<stack>
using namespace std;

/*int main() {
	int getSheep(int n);
	int n, a;
	while (cin >> n) {
		while (n--)
		{
			cin >> a;
			cout << getSheep(a) << endl;
		}
	}
	return 0;
}
int getSheep(int n) {
	int cnt = 3;
	for (int i = 0; i < n; i++) {
		cnt = (cnt - 1) * 2;
	}
	return cnt;
}*/