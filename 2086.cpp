#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#include <set>
#include<math.h> 
#include <iostream>

using namespace std;

/*int main() {
	int n;
	double a, b,tmp;
	double num[3005] = { 0 };
	while (cin >>n)
	{
		cin >> a >> b;
		for (int i = 1; i <= n; i++)
		{
			cin >> num[i];
		}
		double re = n*1.0 / (n*1.0 + 1)*a + 1.0 / (n*1.0 + 1)*b;
		double tmp = 2 * n*1.0;
		for (int j = 1; j <= n; j++) {
			re -= tmp / (n*1.0 + 1)*num[j];
			tmp = tmp - 2;
		}
		cout.precision(2);
		cout <<fixed<< re << endl;
	}
	return 0;
}*/
