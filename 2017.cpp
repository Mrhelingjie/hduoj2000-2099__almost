#include<iostream>
#include<vector>
#include<string>
#include<algorithm>
#include<math.h>
using namespace std;

/*int main(void)
{
	int N, tmp;
	while (cin>>N)
	{
		while (N>0)
		{
			char str[100];
			int sum(0);
			cin >> str;
			for (int i = 0; i <= strlen(str); ++i)
			{
				if (str[i] >= '0'&&str[i] <= '9')
					++sum;
			}
			cout << sum << endl;
			N--;
		}
	}
	return 0;

}


int main(void)
{
	int N, tmp;
	while (cin >> N)
	{
		while (N > 0)
		{
			char str[100];
			int sum(0);
			cin >> str;
			for (int i = 0; i <= strlen(str); ++i)
			{
				if (str[i] >= '0'&&str[i] <= '9')
					++sum;
			}
			cout << sum << endl;
			N--;
		}
	}
	return 0;

}*/


