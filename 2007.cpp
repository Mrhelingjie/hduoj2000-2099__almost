#include<iostream>
#include<vector>
#include<string>
#include<algorithm>
#include<math.h>
#pragma warning(disable:4996)

using namespace std;

/*int main(void)
{

	int N ,M, tmp,sum1,sum2;
	while (scanf("%d%d", &M,&N) != EOF)
	{
		if (M > N)
		{
			tmp = N;
			N = M;
			M = tmp;
		}

		sum1 = 0;
		sum2 = 0;
		for (int i = M; i <= N; i++) {
			if (i % 2 == 0) {
				sum1 = sum1 + i * i;
			}
			else
			{
				sum2 = sum2 + i * i * i;
			}
		}
		
		printf("%d %d\n", sum1,sum2);
	}
	return 0;
}*/