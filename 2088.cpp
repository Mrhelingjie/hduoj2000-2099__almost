#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#include <set>
#include<math.h> 
#include <iostream>

using namespace std;

/*int main() {
	int n, tmp=0;
	int num[51] = { 0 };
	while (cin >>n&&n)
	{
		if (tmp) cout << endl;
		tmp = 1;

		int sum = 0;
		for (int i = 0; i < n; i++) {
			cin >> num[i];
			sum += num[i];
			
		}
		int height = sum / n;
		int cnt = 0;
		for (int i = 0; i < n;i++) {
			if (num[i] < height) {
				cnt += height - num[i];
			}
		}
		cout << cnt << endl;
	}
	return 0;
}*/
