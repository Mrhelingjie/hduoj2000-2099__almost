#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;
/*int main() {
	string insert = "(max)";
	string str;
	while (cin >> str) {
		vector<char> v(str.begin(), str.end());
		sort(v.begin(), v.end());
		char max = v[v.size() - 1];

		string output = "";
		for (int i = 0; i < str.length(); i++) {
			output = output + str[i];
			if (str[i] == max) {
				output.append(insert);
			}
		}
		cout << output << endl;
	}
	return 0;
}*/