#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#include <iomanip>
#include<math.h>
using namespace std;

/*int main() {
	int num;
	bool isPrime(int n);
	while (cin >> num&&num!=0) {
		int mid = num / 2;
		int cnt = 0;
		for (int i = 2; i <mid; i++) {
			if (isPrime(i)&& isPrime(num-i))
			{
				cnt++;
			}
		}
		cout << cnt << endl;
	}
	return 0;
}

bool isPrime(int n) {
	if (n == 1)return false;
	for (int i = 2; i <=sqrt(n); i++) {
		if (n% i == 0) {
			return false;
		}
	}
	return true;
}*/