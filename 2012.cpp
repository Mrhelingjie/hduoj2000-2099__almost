#include<iostream>
#include<vector>
#include<string>
#include<algorithm>
#include<math.h>


using namespace std;

/*int main(void)
{

	int x,y;

	bool isPrime(int num);
	
	while (scanf_s("%d%d", &x,&y) != EOF&&!(x==0&&y==0))
	{
		bool flag = true;
		for (int i = x; i <= y; i++) {
			int tmp = i * i + i + 41;
			if (!isPrime(tmp)) {
				flag = false;
				break;
			}
		}
		if (flag)printf("OK\n");
		else {
			printf("Sorry\n");
		}
	}
	return 0;

}*/

