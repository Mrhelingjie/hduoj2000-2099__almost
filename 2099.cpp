#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#include <set>
#include<math.h> 
#include <iostream>
#include<iomanip>
using namespace std;

/*int main() {
	int m, n;
	
	while (cin >> m >> n && !(m == 0 && n == 0))
	{
		int tmp = m * 100;
		vector<int> v;
		for (int i = 0; i < 100; i++) {
			if ((tmp + i) % n == 0) {
				v.push_back(i);
			}
		}
		
		for (int i = 0; i < v.size(); i++) {
			if (i == v.size() - 1) cout<<setfill('0') << setw(2)<< v[i] << endl;
			else cout << setfill('0') << setw(2) << v[i] << " ";
		}
	}
	return 0;
}*/
