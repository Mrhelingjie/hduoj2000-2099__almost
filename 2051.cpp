#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#include<stack>
#include <iomanip>
using namespace std;

/*int main() {
	int  n, m;

	while (cin >> n) {
		getchar();
		vector<int> v;
		while (n != 0) {
			v.push_back(n % 2);
			n = n / 2;
		}
		vector<int>::reverse_iterator p;
		for (p = v.rbegin(); p != v.rend(); p++) {
			cout << *p ;
		}
		if (v.empty())
		{
			cout << 0 ;
		}
		cout << endl;
	}
	return 0;
}*/


